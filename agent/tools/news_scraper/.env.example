# 2회차 강의: AI 에이전트 기반 뉴스 감성 분석 시스템
# 환경 변수 템플릿 파일
# 실제 사용 시 .env 파일로 복사하여 실제 값을 입력하세요

# =============================================================================
# 애플리케이션 설정 (APP_ 접두사)
# =============================================================================

# 실행 환경 (development, staging, production)
APP_ENVIRONMENT=development

# 디버그 모드
APP_DEBUG=true

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
APP_LOG_LEVEL=INFO

# 로그 파일 경로 (선택적)
APP_LOG_FILE_PATH=logs/app.log

# 배치 처리 크기
APP_BATCH_SIZE=1000

# 워커 프로세스 수
APP_WORKER_COUNT=4

# 암호화 시크릿 키 (프로덕션에서는 반드시 변경)
APP_SECRET_KEY=your-super-secret-key-change-in-production

# =============================================================================
# 데이터베이스 설정 (DB_ 접두사)
# =============================================================================

# MySQL 연결 정보
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_DATABASE=news_sentiment

# 커넥션 풀 설정
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# 디버깅용 SQL 로깅 (개발환경에서만 true)
DB_ECHO=false

# =============================================================================
# 크롤러 설정 (CRAWLER_ 접두사)
# =============================================================================

# 브라우저 타입 (chromium, firefox, webkit)
CRAWLER_BROWSER_TYPE=chromium

# 헤드리스 모드 (true/false)
CRAWLER_HEADLESS=true

# 액션 간 지연시간 (밀리초)
CRAWLER_SLOW_MO=0

# 뷰포트 설정
CRAWLER_VIEWPORT_WIDTH=1920
CRAWLER_VIEWPORT_HEIGHT=1080

# 타임아웃 설정 (밀리초)
CRAWLER_PAGE_TIMEOUT=30000
CRAWLER_NAVIGATION_TIMEOUT=30000
CRAWLER_WAIT_TIMEOUT=10000

# 동시성 설정
CRAWLER_MAX_CONCURRENT_PAGES=5
CRAWLER_MAX_CONCURRENT_CONTEXTS=3

# 재시도 설정
CRAWLER_MAX_RETRIES=3
CRAWLER_RETRY_DELAY=1.0
CRAWLER_EXPONENTIAL_BACKOFF=true

# 사용자 정의 User-Agent (선택적)
# CRAWLER_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# =============================================================================
# API 키 설정
# =============================================================================

# Firecrawl API 키
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# OpenAI API 키 (감성 분석용)
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# 외부 서비스 설정
# =============================================================================

# Redis 연결 (캐싱용, 선택적)
# REDIS_URL=redis://localhost:6379/0

# Elasticsearch 연결 (검색용, 선택적)
# ELASTICSEARCH_URL=http://localhost:9200

# =============================================================================
# 성능 및 모니터링 설정
# =============================================================================

# 메모리 제한 (MB)
MEMORY_LIMIT=1024

# 성능 모니터링 활성화
PERFORMANCE_MONITORING=true

# 지표 수집 간격 (초)
METRICS_INTERVAL=60

# =============================================================================
# 크롤링 대상 사이트 설정
# =============================================================================

# 기본 크롤링 대상 URL (콤마로 구분)
DEFAULT_TARGET_URLS=https://news.naver.com,https://www.donga.com

# 크롤링 간격 (초)
CRAWLING_INTERVAL=300

# 최대 크롤링 페이지 수
MAX_CRAWL_PAGES=50

# =============================================================================
# 감성 분석 설정
# =============================================================================

# 감성 분석 모델 타입 (openai, huggingface, custom)
SENTIMENT_MODEL_TYPE=openai

# 감성 분석 배치 크기
SENTIMENT_BATCH_SIZE=100

# =============================================================================
# 알림 설정 (선택적)
# =============================================================================

# 이메일 설정
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# Slack 웹훅 (오류 알림용)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# =============================================================================
# 보안 설정
# =============================================================================

# CORS 허용 도메인 (콤마로 구분)
ALLOWED_HOSTS=localhost,127.0.0.1

# API 레이트 리미팅
API_RATE_LIMIT=100

# JWT 토큰 만료시간 (초)
JWT_EXPIRATION=3600

# =============================================================================
# 개발 환경 설정
# =============================================================================

# 테스트 모드
TEST_MODE=false

# 더미 데이터 생성
GENERATE_DUMMY_DATA=false

# 프로파일링 활성화
PROFILING_ENABLED=false

# =============================================================================
# 추가 설정 예시
# =============================================================================

# 프록시 설정 (필요시)
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=https://proxy.company.com:8080

# 사용자 정의 설정
# CUSTOM_CONFIG_PATH=/path/to/custom/config.json
